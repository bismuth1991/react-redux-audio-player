
const initialState = {
  playingSongIndex: 1,
  songIds: [1, 2, 3],
};

const audioPlayerReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default audioPlayerReducer;
